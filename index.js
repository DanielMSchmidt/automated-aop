'use strict';
module.exports = function (targets, options) {
	// Validate arguments
	// Build base visitors as far as possible
	return function (input, output) {
		// Validate input and output
		// Read input file and parse it
		// Traverse input file
		// generate output file
	};
};
